# Progress Log

## Learnings
(Patterns discovered during implementation)
- node --check <file> can verify JavaScript syntax without needing dependencies installed
- The `canvas` npm package requires Visual Studio C++ build tools on Windows for native compilation
- Default parameter values in JS function signatures provide backward compatibility

---

## Iteration 1 - US-001: Make Asset Directories Configurable
- What was implemented:
  - Added optional `directories` parameter to `processComboVideo()` function signature
  - Default value set to `['./assets/games/Tekken7/images', './assets/games/common/images']`
  - Removed hardcoded local variable, now uses parameter directly in `searchAndCopyFiles()` call
- Files changed:
  - `src/video/videoUtils.js` (line 32) - function signature updated
  - `src/video/videoUtils.js` (line 59) - uses `directories` parameter instead of local constant
- Learnings for future iterations:
  - The environment has npm dependency issues with native modules (canvas)
  - Tests may not run due to build tool requirements
  - Syntax validation via `node --check` can substitute for full test runs when deps fail
---
